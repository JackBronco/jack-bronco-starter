---
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Jack Bronco</title>
    <style>
      html, body {
        margin: 0;
        padding: 0;
        height: 100%;
        background: #000;
        color: #eaeaea;
        font-family: Georgia, "Times New Roman", serif;
      }

      .container {
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
      }

      .line {
        font-size: clamp(1.4rem, 3vw, 2.2rem);
        letter-spacing: 0.02em;
        opacity: 0;
        animation: fadeIn 3s ease forwards;
      }

      .laughter {
        margin-top: 1.5rem;
        font-size: 1rem;
        opacity: 0;
        animation: fadeIn 3s ease forwards;
        animation-delay: 4s;
      }

      @keyframes fadeIn {
        to { opacity: 1; }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div>
        <div class="line">
          You weren’t supposed to be here yet.
        </div>
        <div class="laughter">
          a low, slow, deep laugh begins to rumble...
        </div>
      </div>
    </div>
  </body>
</html>
